<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Chỉnh sửa người dùng</title>
  <link rel="stylesheet" href="/css/admin-style.css">
</head>
<body>
  <div class="form-container">
    <h2>Chỉnh sửa người dùng</h2>
    <form action="/admin/user/edit/<%= user._id %>" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label>Họ tên</label>
        <input type="text" name="fullName" value="<%= user.fullName %>" required />
      </div>
      <div class="form-group">
        <label>Email</label>
        <input type="email" name="email" value="<%= user.email %>" required />
      </div>
      <div class="form-group">
        <label>Tên đăng nhập</label>
        <input type="text" name="username" value="<%= user.username %>" required />
      </div>
      <div class="form-group">
        <label>Ngành</label>
        <input type="text" name="major" value="<%= user.major %>" />
      </div>
      <div class="form-group">
        <label>Trường</label>
        <input type="text" name="school" value="<%= user.school %>" />
      </div>
      <div class="form-group">
        <label>Mã sinh viên</label>
        <input type="text" name="studentCode" value="<%= user.studentCode %>" />
      </div>
      <div class="form-group">
        <label>Giới thiệu (VI)</label>
        <textarea name="bio[vi]"><%= user.bio?.vi || '' %></textarea>
      </div>
      <div class="form-group">
        <label>Giới thiệu (EN)</label>
        <textarea name="bio[en]"><%= user.bio?.en || '' %></textarea>
      </div>
      <div class="form-group">
        <label>Ảnh đại diện</label>
        <input type="file" name="avatarFile" accept="image/*" />
        <% if (user.avatar) { %>
          <p><img src="<%= user.avatar %>" alt="Avatar" width="100" /></p>
        <% } %>
      </div>
      <div class="form-buttons">
        <button type="submit" class="submit-btn">Lưu</button>
        <a href="/admin" class="cancel-btn">Hủy</a>
      </div>
    </form>
  </div>
</body>
</html>
