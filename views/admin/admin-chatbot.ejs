<!-- Nút bật/tắt chat -->
<button id="admin-chatbot-toggle">💬</button>

<!-- Khung chat -->
<div id="admin-chatbot-window">
  <div id="admin-chatbot-header">
    <div class="header-content">
      <div class="avatar">👨‍💼</div>
      <span>Admin Assistant</span>
    </div>
    <button class="maximize-btn" id="admin-maximize-btn">⤢</button>
  </div>
  <div id="admin-chatbot-messages" class="messages-container"></div>
  <div id="admin-chatbot-input">
    <input type="text" id="admin-user-input" placeholder="Nhập tin nhắn..." />
    <button id="admin-send-btn">
      <span>Gửi</span>
      <span>➤</span>
    </button>
  </div>
</div>

<style>
  * {
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }
  #admin-chatbot-toggle {
    position: fixed;
    bottom: 100px;
    right: 20px;
    background-color: #2c3e50;
    color: white;
    border: none;
    border-radius: 50%;
    width: 60px;
    height: 60px;
    font-size: 28px;
    cursor: pointer;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.25);
    transition: background-color 0.3s;
    z-index: 1000;
  }
  #admin-chatbot-toggle:hover {
    background-color: #34495e;
  }
  #admin-chatbot-window {
    position: fixed;
    bottom: 100px;
    right: 90px;
    width: 380px;
    height: 520px;
    background: #f8f9fa;
    border-radius: 16px;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2);
    display: none;
    flex-direction: column;
    overflow: hidden;
    border: 1px solid #dee2e6;
    z-index: 1000;
    transition: all 0.3s ease;
  }
  #admin-chatbot-window.maximized {
    width: 80%;
    height: 95%;
    right: 5%;
    bottom: 5%;
    border-radius: 8px;
  }
  #admin-chatbot-header {
    background: linear-gradient(135deg, #2c3e50, #1a252f);
    color: white;
    padding: 15px;
    font-weight: bold;
    font-size: 16px;
    text-align: center;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }
  .header-content {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .maximize-btn {
    background: none;
    border: none;
    color: white;
    cursor: pointer;
    padding: 5px;
    font-size: 16px;
    transition: transform 0.3s;
  }
  .maximize-btn:hover {
    transform: scale(1.1);
  }
  #admin-chatbot-messages {
    flex: 1;
    padding: 15px;
    overflow-y: auto;
    background-color: #fff;
    display: flex;
    flex-direction: column;
  }
  .message-container {
    display: flex;
    align-items: flex-start;
    margin: 8px 0;
    gap: 8px;
    width: 100%;
  }
  .message-container.user {
    flex-direction: row-reverse;
  }
  .avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background-color: #e9ecef;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 16px;
    flex-shrink: 0;
  }
  .bot .avatar {
    background-color: #2c3e50;
    color: white;
  }
  .message {
    padding: 8px 12px;
    border-radius: 15px;
    line-height: 1.4;
    word-wrap: break-word;
    max-width: calc(100% - 50px);
    white-space: pre-wrap;
  }
  .user .message {
    background-color: #e9ecef;
    border-bottom-right-radius: 5px;
  }
  .bot .message {
    background-color: #2c3e50;
    color: white;
    border-bottom-left-radius: 5px;
  }
  .typing-indicator {
    display: flex;
    gap: 4px;
    padding: 8px 12px;
  }
  .typing-dot {
    width: 8px;
    height: 8px;
    background: #2c3e50;
    border-radius: 50%;
    animation: typing 1s infinite ease-in-out;
  }
  .typing-dot:nth-child(2) { animation-delay: 0.2s; }
  .typing-dot:nth-child(3) { animation-delay: 0.4s; }
  @keyframes typing {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-5px); }
  }
  #admin-chatbot-input {
    display: flex;
    border-top: 1px solid #ccc;
    background: #fff;
    padding: 10px;
  }
  #admin-chatbot-input input {
    flex: 1;
    padding: 10px;
    border: 1px solid #ced4da;
    border-radius: 20px;
    outline: none;
    font-size: 14px;
    margin-right: 10px;
    transition: border-color 0.2s;
  }
  #admin-chatbot-input input:focus {
    border-color: #2c3e50;
  }
  #admin-chatbot-input button {
    background: #2c3e50;
    color: white;
    border: none;
    border-radius: 20px;
    padding: 10px 16px;
    cursor: pointer;
    font-size: 14px;
    transition: background 0.3s;
    display: flex;
    align-items: center;
    gap: 5px;
  }
  #admin-chatbot-input button:hover {
    background: #34495e;
  }
  .error-message {
    color: #dc3545;
    font-size: 12px;
    padding: 8px 12px;
    background: #f8d7da;
    border-radius: 10px;
    margin: 4px 0;
  }
  .welcome-message {
    background: #f8f9fa;
    color: #2c3e50;
    padding: 20px;
    margin: 15px;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 4px 15px rgba(44, 62, 80, 0.1);
    border: 1px solid #e9ecef;
  }
  .welcome-message h3 {
    margin: 0 0 10px 0;
    font-size: 18px;
    font-weight: 600;
    color: #2c3e50;
  }
  .welcome-message p {
    margin: 0;
    font-size: 14px;
    color: #495057;
    line-height: 1.5;
  }
</style>

<script>
  const toggleBtn = document.getElementById("admin-chatbot-toggle");
  const chatWindow = document.getElementById("admin-chatbot-window");
  const messages = document.getElementById("admin-chatbot-messages");
  const userInput = document.getElementById("admin-user-input");
  const sendBtn = document.getElementById("admin-send-btn");
  const maximizeBtn = document.getElementById("admin-maximize-btn");

  // Thêm tin nhắn chào mừng
  function addWelcomeMessage() {
    const welcomeDiv = document.createElement("div");
    welcomeDiv.className = "welcome-message";
    welcomeDiv.innerHTML = `
      <h3>👋 Xin chào Admin!</h3>
      <p>Tôi là trợ lý ảo của hệ thống. Tôi có thể giúp gì cho bạn?</p>
    `;
    messages.appendChild(welcomeDiv);
    messages.scrollTop = messages.scrollHeight;
  }

  toggleBtn.addEventListener("click", () => {
    const isOpening = chatWindow.style.display === "none";
    chatWindow.style.display = isOpening ? "flex" : "none";
    
    if (isOpening) {
      // Xóa tin nhắn cũ khi mở lại
      messages.innerHTML = '';
      // Thêm tin nhắn chào mừng
      addWelcomeMessage();
      userInput.focus();
    }
  });

  // Xử lý nút phóng to
  maximizeBtn.addEventListener("click", () => {
    chatWindow.classList.toggle("maximized");
    maximizeBtn.textContent = chatWindow.classList.contains("maximized") ? "⤢" : "⤡";
  });

  sendBtn.addEventListener("click", async () => {
    const msg = userInput.value.trim();
    if (!msg) return;
    appendMessage("user", msg);
    userInput.value = "";

    // Hiển thị typing indicator
    const typingContainer = document.createElement("div");
    typingContainer.className = "message-container bot";
    typingContainer.innerHTML = `
      <div class="avatar">👨‍💼</div>
      <div class="typing-indicator">
        <div class="typing-dot"></div>
        <div class="typing-dot"></div>
        <div class="typing-dot"></div>
      </div>
    `;
    messages.appendChild(typingContainer);
    messages.scrollTop = messages.scrollHeight;

    try {
      const response = await fetch("/api/admin/chat", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ message: msg }),
      });
      // Xóa typing indicator
      messages.removeChild(typingContainer);
      const data = await response.json();
      if (data.error) {
        appendErrorMessage(data.error);
      } else {
        appendMessage("bot", data.reply || "Xin lỗi, tôi không hiểu câu hỏi của bạn.");
      }
    } catch (error) {
      // Xóa typing indicator
      messages.removeChild(typingContainer);
      appendErrorMessage("Lỗi kết nối đến máy chủ");
    }
  });
  
  userInput.addEventListener("keypress", function (e) {
    if (e.key === "Enter") {
      sendBtn.click();
    }
  });

  function appendMessage(sender, text) {
    const container = document.createElement("div");
    container.className = `message-container ${sender}`;
    
    const avatar = document.createElement("div");
    avatar.className = "avatar";
    avatar.textContent = sender === "user" ? "👤" : "👨‍💼";
    
    const messageDiv = document.createElement("div");
    messageDiv.className = "message";
    messageDiv.textContent = text || "...";
    
    container.appendChild(avatar);
    container.appendChild(messageDiv);
    messages.appendChild(container);
    messages.scrollTop = messages.scrollHeight;
  }

  function appendErrorMessage(text) {
    const errorDiv = document.createElement("div");
    errorDiv.className = "error-message";
    errorDiv.textContent = text;
    messages.appendChild(errorDiv);
    messages.scrollTop = messages.scrollHeight;
  }
</script> 